from django.urls import path
from . import views

urlpatterns = [
    # Example view path (Uncomment if you have views to add)
    # path('', views.index, name='index'),  # Home page or default view
]